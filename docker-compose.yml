services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env.example
    environment:
      DATABASE_URL: sqlite:////data/app.db
      UPLOAD_DIR: /data/uploads
    volumes:
      - ./data:/data
    restart: unless-stopped
